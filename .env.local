NEXT_PUBLIC_HASHNODE_GQL_ENDPOINT=https://gql.hashnode.com
NEXT_PUBLIC_HASHNODE_PUBLICATION_HOST=finntranter.hashnode.dev   # ← Change to your actual Hashnode publication handle (check in Hashnode dashboard → Settings → Domains or Profile)
NEXT_PUBLIC_BASE_URL=/blog                                       # ← This enables subpath mode (change to /articles if preferred)
NEXT_PUBLIC_MODE=production
